# 202BHW4

Clone this github repo onto your machine. Navigate to the directory that contains the repo and go inside the folder. When inside the folder simply run the command:
python3 -m pipeline (seed #) to recreate the entire experiment including plots, and hyper-parameter search. The default seed value is 42, but if you wanted a seed of 123 you would run
python3 -m pipeline 123
